# Local Netlify folder
.netlify

# Dependencies
node_modules

# Local dev only (never deploy)
.env
devserver.py
deno.lock
__pycache__/

# Local dev accounts (secrets — never commit)
.local-dev-accounts.json
